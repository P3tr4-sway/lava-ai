# playback

Thin wrapper over alphaTab's AlphaSynth player.

- `player.ts` — `play()`, `pause()`, `stop()`, `seek(tick)`, `setSpeed(0.25–2)`, `setLoop(from, to)`, `setMetronome(on)`, `setCountIn(on)`. Subscribes to `playerStateChanged` and `playerPositionChanged`.
