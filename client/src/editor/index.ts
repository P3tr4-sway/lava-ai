/**
 * EditorCore façade — single entry point for the editor core.
 *
 * Phases 3–6 will populate this with the concrete implementations.
 * For now it re-exports the module namespaces so consumers can
 * import from '@/editor' without knowing internal paths.
 */

// Phase 3 — AlphaTex AST (implemented in Phase 3)
// export * from './ast/types'

// Phase 4 — Commands & History (implemented in Phase 4)
// export * from './commands/Command'
// export * from './history/History'

// Phase 6 — Selection / Cursor (implemented in Phase 6)
// export * from './selection/SelectionModel'
